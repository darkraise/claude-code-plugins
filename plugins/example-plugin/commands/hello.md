---
description: Say hello and confirm the example plugin is installed
---

Greet the user warmly (by name if you know it). Then confirm that `example-plugin`
from the `darkraise` marketplace is installed and working, and point them to
`plugins/example-plugin/` as the template to copy when creating a new plugin.
